{
  "name": "publi-con-jorge",
  "private": true,
  "description": "Landing y membresías de Publi con Jorge",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "node -e \"console.log('No frontend build')\"",
    "vercel-build": "node -e \"console.log('Static public/ output, no frontend compile')\""
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pdfkit": "^0.17.1",
    "stripe": "^18.2.1"
  }
}
